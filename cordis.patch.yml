# dsh-stream-rules bundle layer: inserts the stream-rules plugin row.
# Override options in your profile's later cordis.patch.yml under id: stream-rules
- insert:
    - id: stream-rules
      name: '@jiesou/dsh-stream-rules'
